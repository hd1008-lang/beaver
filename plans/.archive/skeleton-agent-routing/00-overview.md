# skeleton-agent-routing — Overview

## Goal

Add the `## Agent Routing` table and PARK RULE section to the `claudeMdTemplate` in all three harness-only skeleton files, so CLAUDE.md generated by the harness-only flow is consistent with the react-vite full scaffold output.

## Scope

- `src/scaffold/harness-only/templates/generic-skeleton.ts`
- `src/scaffold/harness-only/templates/react-vite-skeleton.ts`
- `src/scaffold/harness-only/templates/chrome-extension-skeleton.ts`

Reuse the existing `claudeHarnessTableTemplate()` export from `src/scaffold/shared/claude-setup.ts`.

## Non-goals

- No changes to `buildClaudeFileMap` or `ClaudeHarnessParams`
- No changes to agent templates or conventions skills inside the skeleton files
- No changes to full scaffold templates (`react-vite/templates/claude-setup.ts`, `chrome-extension/templates/claude-setup.ts`)
- No spec changes (spec already says harness-only skeletons should have Agent Routing; the code was just missing it)
- No dogfood sync needed (no changes to `src/scaffold/shared/claude-setup.ts`)

## Memory note to update

The planner MEMORY.md contains an incorrect note: "Harness-only skeleton CLAUDE.md stubs are intentionally minimal — they tell users to run `claude /init`. Never add full agent-routing tables or park-rule lines." This was wrong — the spec mandates the Agent Routing table. Update the memory note as part of the final phase.

## Ordered phases

| # | Phase | Status | Steps | Updated |
|---|---|---|---|---|
| 01 | update-skeletons | done | 5/5 | 2026-06-21 |
| 02 | verify-and-close | done | 5/5 | 2026-06-21 |
